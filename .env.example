# .env.example

# 这是一个环境变量的示例文件。
# 在你的本地开发环境中，请复制此文件为 .env，并填入你的配置信息。
# .env 文件不应被提交到 Git 仓库中。

# --- 数据库配置 (PostgreSQL) ---
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=warehouse_system_db
DB_PORT=5432

# --- 安全配置 ---
# 用于生成和验证 JWT (JSON Web Tokens) 的密钥
# 推荐使用一个长且随机的字符串，例如通过 `openssl rand -base64 32` 生成
JWT_SECRET=your_jwt_secret_here

# --- 默认管理员账户 ---
# 用于在数据库初始化时创建的第一个管理员账户
ADMIN_USER=admin
ADMIN_PASSWORD=your_strong_admin_password_here

# --- Node.js 环境 ---
NODE_ENV=development
PORT=3001

# --- Production Environment Specific ---
# The public URL of your deployed frontend.
# Used by the backend to configure CORS policy.
# Example: https://my-warehouse-app.com
FRONTEND_URL=